
#include <stdio.h>
#include <stdlib.h> 

int
main(int argc, char* argv[])
{
  if (argc != 2)
  {
    printf("usage: hex <int>\n");
    exit(1); 
  }

  int i;
  sscanf(argv[1], "%i", &i);
  printf("%X\n", i);

  return 0; 
}
