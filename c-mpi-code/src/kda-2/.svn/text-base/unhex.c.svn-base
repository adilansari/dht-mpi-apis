
#include <stdio.h>
#include <stdlib.h> 

int
main(int argc, char* argv[])
{
  if (argc != 2)
  {
    printf("usage: unhex <hex>\n");
    exit(1); 
  }

  int x;
  sscanf(argv[1], "%X", &x);
  printf("%i\n", x);

  return 0; 
}
